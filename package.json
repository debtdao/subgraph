{
  "name": "debt-dao",
  "license": "UNLICENSED",
  "scripts": {
    "prebuild": "graph codegen",
    "build": "graph build",
    "predeploy-test": "yarn run build --network goerli --network-file ./networks.json",
    "deploy-test": "graph deploy --product hosted-service kibagateaux/dd-test",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ kibagateaux/dd-test",
    "create-local": "graph create --node http://localhost:8020/ kibagateaux/dd-test",
    "remove-local": "graph remove --node http://localhost:8020/ kibagateaux/dd-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 kibagateaux/debt-dao"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.2",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
