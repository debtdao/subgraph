{
  "name": "debt-dao",
  "license": "UNLICENSED",
  "scripts": {
    "prebuild": "graph codegen",
    "build": "graph build --network ",
    "predeploy-test": "yarn run build goerli --network-file ./networks.json",
    "deploy-test": "graph deploy --product hosted-service hepnerthomas/dd-test",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ hepnerthomas/dd-test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.2",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
